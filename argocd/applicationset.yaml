---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd-demo-applicationset
  namespace: argocd-demo
spec:
  generators:
    - git:
        repoURL: https://github.com/christianknell/argocd-demo.git
        revision: main
        directories:
          - path: charts/*
  template:
    metadata:
      name: "{{`{{ path.basename }}`}}"
    spec:
      project: argocd-demo
      source:
        repoURL: https://github.com/christianknell/argocd-demo.git
        targetRevision: main
        path: "{{`{{ path }}`}}"
        helm:
          releaseName: "{{`{{ path.basename }}`}}"
          valueFiles:
            - values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd-demo
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
